VITE_CIRRUS_API_BASE_URL=http://localhost:4000

# DAMPLab Canvas UI — separate app, deployed via cumulab/canvas-deploy
# (`npm run deploy:canvas` in cirrus-backend). Opened in a new tab from the
# dashboard's Canvas card.
VITE_CANVAS_URL=http://localhost:8088
